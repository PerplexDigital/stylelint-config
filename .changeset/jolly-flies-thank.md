---
'@perplex-digital/stylelint-config': minor
---

replace `stylelint-config-standard` with `@dreamsicle.io/stylelint-config-tailwindcss` in the list of extended configs in README.md and src/index.js, and remove `stylelint-config-standard` from dependencies in package.json.
